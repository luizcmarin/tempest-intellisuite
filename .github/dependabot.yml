version: 2
updates:
  # npm dependencies (package.json)
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      dev-dependencies:
        dependency-type: development

  # Actions used by the CI workflows
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
